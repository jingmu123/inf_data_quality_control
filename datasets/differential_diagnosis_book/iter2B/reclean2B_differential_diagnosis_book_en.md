## reclean1B_differential_diagnosis_book问题
### 无关文本：
1. 括号内无关文本补充，(Knowles & Sharma， 2004， p.599)、(King， 1994， p. 6； see the phenomenon of forced normalization， chapter 6)等。删除2修改补充。
```
[r'([\(（][^（）\(\)]*([Ff]i ?g ?\.?(ure)?|[Ss]ee|[Pp]age|[Ee]xhibit|[Pp]icture|[Cc]hapter|[Bb]ox|[Tt]able|[，；,; ]+ ?\d{4}[ ，；,; ]+|[ ，；,;]p\. ?\d+)[^（）\(\)]*[）\)])', r'删除2:<u>\1</u>'],
```

2. 段末无关数字，of immune dysregulation.123、the under-lying malignancy.120，122等。
```
[r'([a-z]\.)( ?[\|\d]+([， \.\-\|]\d*)*$)', r'\1删除14:<u>\2</u>'],
```

3. 零碎简短的无关段落， **b，c**，**6.51**删除3补充。
```
[r'(^[\*_]*(\w{1,2}( \w{1,2})+|(\w[，\.]?\w{0,3})|[\d_ ]+)[\*_]*$)', r'删除3:<u>\1</u>'], 
```

4. 参考文献年份特征补充，补充名字后出现的括号年份及句号，Caine，E.D. (1986).、 A. (1997， March).、Karzmark，P， (1992).等。
```
r'([，,；;][A-Za-z_ ]*\d{4}[，,\._]+)|([A-Z][\.，] ?(\(\d{4}\)|\(\d{4}，[^\(\)]*\))\.)|([\.;；：:，, ]\d{4}[;；：:，,\.])',  # 年份
```

5.参考文献页码特征补充，补充数字后出现的括号页码范围包括英文的情况，oEngineering.57(9-B)，5916.、cientific bases (pp. 123-140). 等。
```
r'(\d+(\(\d+\))?[：:；; ,，]+[a-zA-Z]?\d+)|([a-zA-Z\d]{1,4}[\-–][a-zA-Z\d]{1,4}[，,\.])|(\d+\([a-zA-Z\d]{1,4}([\-–][a-zA-Z\d]{1,4})?\)[：:；; ,，])|(\([^\(\)]*p[p\. ]*\d+[\-–][a-zA-Z\d]{1,4}\)[，,\.])',  # 页码范围格式
```

6. 无关标题，**Suggested Reading**、删除5直接补充。
```
[r'(^[\\\*_ ]*(\SUGGESTED READING|Suggested Reading)[\*_]*$)',  r'删除5:<u>\1</u>'],  
```

7. 图注段落内容出现单个换行符，导致漏删，补充单个\n。
```
r'(^[#\*_]*\s?([Ff]igs?(ure)?|F\s?IGS?(URE)?).*\n?.*)', r'通用删除16(英):<u>\1</u>'],  # 删除开头为Figure的描述
```

### 错误删除：
1. move_ref删除参考文献会误删正文段落，补充添加字符限制。
```
    if (p_sum >= 3 and len(con) < 400) or (p_sum == 2 and len(con) < 200):
        con = "参考删除-1:<u>{}</u>".format(con)
```

2. 删除3误删较多，将其修改为最多为两个字符相连的零碎段落。
```
[r'(^[\*_]*(\w{1,2}( \w{1,2})+|(\w[，\.]?\w{0,1})|[\d_ ]+)[\*_]*$)', r'删除3:<u>\1</u>'],  # 无关零碎段，B、a bC、_a_ bC、C21等
```

3. page_number_duan无关页码删除误删较多，取消删除简短的段落。

### 多余换行：
1. 上段字符长度小于45但结尾为\-的，与下段小写开头，**Fig.6.71a-c. Small-bowel ob-（\n换行\n）struction due to adhesions. 补充删除换行0。将\-结尾的上段字符长度限制取消。
```
context = re.sub(r'(\-|[^|\n]{45,}[A-Za-z，\d→\)])([ \*]*\n+\n[ \*]*)(([\“a-z0&\(]|\d+[^\.\d\\\)s]).{45,})', r'\1|删除0换行|\3', context)
```




